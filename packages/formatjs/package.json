{
  "name": "formatjs-repo",
  "version": "1.0.0",
  "private": true,
  "description": "This repository is the home of FormatJS and related libraries.",
  "repository": {
    "type": "git",
    "url": "https://github.com/formatjs/formatjs"
  },
  "scripts": {
    "build:bootstrap": "lerna bootstrap --hoist --include-filtered-dependencies --no-ci",
    "build:packages": "lerna run clean --stream && lerna run build --stream",
    "build": "npm run build:bootstrap && npm run prettier && npm run build:packages",
    "cover:collect": "mkdir -p ./.nyc_output/ && for d in $(find packages -type d -name '.nyc_output' -maxdepth 2 -exec find '{}' -type f ';'); do (cp $d ./.nyc_output/); done; nyc report --reporter=lcov --report-dir=${COVERAGE_DIR:-artifacts/coverage}",
    "cover": "lerna run cover --since",
    "dev:lint": "lerna run lint --since master --stream",
    "dev:test": "lerna run test --since master --stream",
    "karma:ci": "karma start karma.conf-ci.js",
    "karma:local": "karma start karma.conf.js",
    "lint": "eslint .",
    "prepublishOnly": "npm run build",
    "prettier": "prettier --write 'packages/*/{test,tests,src}/**/*.ts' '{src,scripts,test,tests}/*.js'",
    "test": "lerna run lint --stream && TS_NODE_COMPILER_OPTIONS='{\"module\": \"commonjs\"}' lerna run test --stream"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "@babel/preset-react": "^7.0.0",
    "@istanbuljs/nyc-config-typescript": "^0.1.3",
    "@types/benchmark": "^1.0.31",
    "@types/chai": "^4.1.7",
    "@types/fs-extra": "^7.0.0",
    "@types/mocha": "^5.2.7",
    "babel-jest": "^24.8.0",
    "benchmark": "^2.1.4",
    "chai": "^4.2.0",
    "coveralls": "^3.0.4",
    "cross-env": "^5.2.0",
    "eslint": "^5.16.0",
    "eslint-plugin-react": "^7.12.4",
    "fast-memoize": "^2.5.1",
    "fs-extra": "^8.0.1",
    "full-icu": "^1.3.0",
    "glob": "^7.1.4",
    "intl": "^1.2.5",
    "intl-pluralrules": "^1.0.2",
    "jest": "^24.8.0",
    "karma": "^4.1.0",
    "karma-chai": "^0.1.0",
    "karma-chrome-launcher": "^2.2.0",
    "karma-mocha": "^1.3.0",
    "karma-sauce-launcher": "^2.0.2",
    "lerna": "^3.15.0",
    "mocha": "^6.1.4",
    "mocha-lcov-reporter": "^1.3.0",
    "moment": "^2.8.2",
    "nyc": "^14.0.0",
    "pegjs": "^0.10.0",
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3",
    "rollup": "^1.15.1",
    "rollup-plugin-commonjs": "^10.0.0",
    "rollup-plugin-node-resolve": "^5.0.1",
    "rollup-plugin-typescript2": "^0.21.1",
    "rollup-plugin-uglify": "^6.0.2",
    "source-map-support": "^0.5.12",
    "ts-node": "^8.1.0",
    "tslib": "^1.10.0",
    "typescript": "^3.4.5"
  },
  "devEngines": {
    "node": "8.x || 10.x || 12.x",
    "npm": "6.x"
  },
  "pre-commit": [
    "dev:lint",
    "dev:test"
  ],
  "author": "Seth Bertalotto <sbertal@verizonmedia.com>",
  "license": "BSD-3-Clause",
  "prettier": {
    "parser": "typescript",
    "singleQuote": true,
    "endOfLine": "lf"
  }
}
