load("//tools:package_json.bzl", "generate_package_json")
load("//tools:vitest.bzl", "vitest")

generate_package_json(
    package_name = "integration-tests",
    private = True,
)

# Manually-managed integration test: bespoke vitest() with custom srcs/deps.
# Auto-generated formatjs_library would conflict and require a checked-in
# tsconfig.json this dir doesn't have.
# gazelle:ts_enabled false

# vitest(
#     name = "integration_test",
#     srcs = glob([
#         "integration/*.ts*",
#     ]) + [
#         "//:tsconfig",
#     ],
#     deps = [
#         "//:node_modules/@formatjs/ts-transformer",
#         "//:node_modules/@types/react",
#         "//:node_modules/@types/react-dom",
#         "//:node_modules/react",
#         "//:node_modules/react-dom",
#         # ts-jest deps
#         ":node_modules/ts-jest",
#     ],
# )

vitest(
    name = "vue_integration_test",
    srcs = [
        "vue/integration.test.ts",
        # ts-loader deps
        "//:tsconfig",
    ],
    fixtures = glob([
        "vue/fixtures/*",
    ]),
    flaky = True,
    deps = [
        "//:node_modules/@babel/types",
        "//:node_modules/@formatjs/ts-transformer",
        "//:node_modules/@types/webpack",
        "//:node_modules/ts-loader",
        "//:node_modules/typescript",
        "//:node_modules/vue",
        "//:node_modules/vue-class-component",
        "//:node_modules/vue-loader",
        "//:node_modules/webpack",
    ],
)
