load("@npm//:defs.bzl", "npm_link_all_packages")
load("//tools:vitest.bzl", "vitest")

npm_link_all_packages()

vitest(
    name = "integration_test",
    srcs = glob(["*.ts"]),
    fixtures = glob(["fixtures/*"]),
    deps = [
        ":node_modules/@formatjs/vite-plugin",
        "//:node_modules/@types/react",
    ],
)
