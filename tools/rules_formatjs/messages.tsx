import {defineMessage} from 'react-intl'

defineMessage({
  id: 'rules-formatjs.local-cli',
  defaultMessage: 'Built from the local FormatJS CLI',
  description: 'Validates the monorepo toolchain',
})
